$(document).ready(function() {
  var animalselection = $("#giraffe-button").click();
  var animalselection = $("hippo-button").click();
  var animalselection = $("elephant-button").click();

  if anim
});
